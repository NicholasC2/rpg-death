{
    "name": "rpg-death",
    "version": "0.0.1",
    "scripts": {
        "dev": "npx esbuild src/index.ts --bundle --minify --outfile=dist/app.js --platform=browser --format=iife --watch"
    },
    "devDependencies": {
        "esbuild": "^0.28.1",
        "typescript": "^6.0.3"
    }
}
